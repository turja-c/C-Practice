#include <stdio.h>

int main() {
    // temperature
    // float C, F;
    // printf("Enter the current temperature in C: ");
    // scanf("%f", &C);
    // F = (C * 1.8) + 32;
    // printf("The temperature in Fahrenheit is '%f'", F);
    // return 0;

    // length
    // float cent, kilo, met;
    // printf("Enter centimeters: ");
    // scanf("%f", &cent);
    // met = cent/100;
    // kilo = cent/100000;
    // printf("Length in meters: %f\n", met);
    // printf("Lenght in kilometeres: %f", kilo);
    // return 0;

    //time
    // int day, year, week, days;
    // printf("Enter the number of days: ");
    // scanf("%d", &days);
    // year = days / 365;
    // week = (days % 365) / 7;
    // days  = days - ((year * 365) + (week * 7));
    // printf("%d year/s, \n %d week/s, \n %d day/s", year, week, days);
    // return 0;
}