#include <stdio.h>

int main() {
    
    // #rectangle area
    // float length, width, area;
    // printf("Enter the length: ");
    // scanf("%f", &length);
    // printf("Enter the width: ");
    // scanf("%f", &width);
    // area = length * width; 
    // printf("the area of the rectangle is %f units", area);

    // #rectangle perimeter
    // float length, width, perimeter;
    // printf("Enter the length: ");
    // scanf("%f", &length);
    // printf("Enter the width: ");
    // scanf("%f", &width);
    // perimeter = length + width; 
    // printf("the perimeter of the rectangle is %f units", perimeter);

    // #triangle area
    // float length, width, area;
    // printf("Enter the length: ");
    // scanf("%f", &length);
    // printf("Enter the width: ");
    // scanf("%f", &width);
    // area = (length * width)/2; 
    // printf("the area of the triangle is %f units", area);
    // return 0;

    // #triangle angles
    // float firstang, secondang, last;
    // printf("Enter the first angle: ");
    // scanf("%f", &firstang);
    // printf("Enter the second angle: ");
    // scanf("%f", &secondang);
    // last = 180 - (firstang + secondang);
    // printf("the last angle of the triangle is %f units", last);
    // return 0;

}